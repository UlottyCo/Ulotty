{
  "buildCommand": "SKIP_BUILD_ERRORS=true npm run build",
  "env": {
    "SKIP_BUILD_ERRORS": "true"
  },
  "regions": ["sfo1"],
  "functions": {
    "api/**": {
      "maxDuration": 30
    }
  }
}
